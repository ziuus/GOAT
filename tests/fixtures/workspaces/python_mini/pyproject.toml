[tool.poetry]
name = "python-mini"
version = "0.1.0"
description = ""
authors = ["Test <test@example.com>"]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
